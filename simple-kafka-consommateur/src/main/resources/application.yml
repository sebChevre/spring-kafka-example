#kafka endpoint
kafka:
  bootstrapAddress: localhost:9092

#Topic de test
test:
  topic:
    name: kafka-topic-json2
  retry:
    topic:
      name: kafka-topic-json2-retry

#Spring profiles
spring:
  profiles:
    active: vm
